import styled from "styled-components";

export const AuthFormError = styled.div`
	font-size: 18px;
	font-weight: 500;
	letter-spacing: 1px;
	color: ${(props) => props.theme.colors.authFormColorText};
`;
